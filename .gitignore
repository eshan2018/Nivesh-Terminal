# Secrets — NEVER commit API keys
.streamlit/secrets.toml
.env
.env.*

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg

# Python tooling caches (backend, M1+)
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# OS
.DS_Store
Thumbs.db

# IDE / tools
.idea/
.vscode/
*.swp
*.swo
.claude/

# Virtual environments
venv/
.venv/
env/

# Node / Next.js (Vercel migration — see MIGRATION.md)
node_modules/
web/node_modules/
web/.next/
web/out/
.vercel/
*.tsbuildinfo

# Next.js generated types
web/next-env.d.ts

# Local domain store created by the API entry point (backend/main.py, ED-003).
# Development artifact: the production system of record is PostgreSQL (ADR-0008).
*.sqlite3
*.sqlite3-journal

# Live ingestion output (M6b-2). Deliberately NOT committed: bulk vendor payloads are
# the redistribution question doc 14 / PROJECT_CONTEXT open item 1 defers to a
# Licensing Strategy discussion, and committing them would prejudge it. Run manifests
# are per-run operational output, not repository knowledge.
raw-store/
ingest-manifest-*.json
